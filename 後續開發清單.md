# 後續開發清單

> 更新日期：2026-04-20｜分支：dev-Ataya  
> Session 3 已完成：菜單動態渲染、月報表 API 串接、Regions upsert 更新、後端三分支新檔合併。

---

## ✅ Session 3 本次完成

| 項目 | 說明 |
|------|------|
| 後端 dev-xiang 合併 | `LoginInterceptor.java` + `WebConfig.java`（Session 攔截器）|
| 後端 dev-kao 合併 | 月報表全套：entity / dao / service / controller + Req / Res + logback-spring.xml |
| 後端 dev-wun 合併 | `UpsertRegionsTaxReq`, `UpdateRegionsUsageCapReq`, `PhoneValidatorUtils` + 更新版 RegionsController/Service/Dao/Entity/Res |
| `api.config.ts` | REGIONS 端點改為 `upsert` + `update_usage_cap`；新增 REPORTS（3 端點）|
| `api.service.ts` | RegionVO 新增 `countryCode`/`usageCap`；新增 Upsert / Reports 介面與方法 |
| 客戶端菜單動態渲染 ✅ | `menuItems` signal + `menuCategories` + `getItemsByCategory()`；HTML 改為 `@for` 迴圈；ngOnInit 呼叫 `getActiveProducts()` |
| `manager-dashboard.component.ts` | `createRegion` / `updateRegion` → `upsertRegion`（含 countryCode）|

---

## 🟡 中優先（Demo 後第一週）

### 1. 月報表頁面（管理端 finance tab）

**位置：** `src/app/manager-dashboard/manager-dashboard.component`

**現況：** `ApiService` 已有 `getMonthlyReport()`、`getMonthlyReportByRange()`、`getRevenueReports()` 三個方法（dev-kao 串接完成），但管理端 finance tab 目前無報表區塊。

**串接步驟：**
1. 在 manager-dashboard 的 finance tab 新增月份選擇器 + 報表列表
2. 呼叫 `apiService.getMonthlyReport({ reportDate: '2026-04' })` 取得本月 + 上月對比
3. 可選：新增日期區間選擇器，呼叫 `getRevenueReports()` 顯示每日趨勢圖

**後端 API 端點（需員工 Session）：**
- `POST find_monthly_reports` → `MonthlyReportRes`（currentData + lastData）
- `POST find_monthly_reports_by_date_range` → `MonthRangeReportsRes`
- `POST get_revenue_reports` → `RevenueQueryRes`（支援分店 / 國家 / 全球維度）

---

### 2. 菜單圖片串接

**位置：** `customer-home.component.html` 的各 `.menu-item-img` class

**現況：** 圖片使用 CSS 背景圖，非動態載入。

**方案：**
```ts
// 每張商品卡加入 [style.background-image] 綁定
getProductImageUrl(id: number): string {
  return `${API_CONFIG.BASE_URL}/lazybaobao/products/${id}/image`;
}
```
後端 `GET lazybaobao/products/{id}/image` 回傳 Base64，前端用 `data:image/jpeg;base64,` prefix 顯示。

---

### 3. BranchService 加入 globalAreaId

**位置：** `src/app/shared/branch.service.ts`

**問題：** `BranchService` 目前只管 country（TW/JP/KR），沒有 `globalAreaId` 欄位，
導致菜單 API 只能 hardcode `globalAreaId = 1`。

**做法：** 在 `CountryConfig` 或 `BranchService` 加入 `globalAreaId: number`，
並在 QR Entry 時由 URL 參數帶入（`?area=1`）。

---

## 🟢 低優先（長期規劃）

### 4. 客戶端歷史訂單頁串接

- 當前：mock 假資料固定顯示
- `getAllOrders({ memberId })` 已在 ngOnInit 中呼叫，但需要 memberId 為真實後端 ID
- 依賴：Member 登入系統完整串接

### 5. 客戶端廚房狀態輪詢

- `getOrderStatus(id, orderDateId)` API 已實作
- 目前前端有 `statusPollInterval`，邏輯已寫，串接點在 `startStatusPolling()` 方法

### 6. 員工登入 Session 持久化

- 目前員工登入為 `AuthService` mock，重新整理後登入狀態消失
- 需串接 `staffLogin()` + Cookie/Session 儲存

### 7. 匯率顯示頁面

- `getAllRates()` 和 `getRatesByDate()` API 已實作
- 管理端 `finance` tab 目前無匯率區塊，可新增

---

## ✅ 累計已完成項目

### Session 1（後端 + API 基礎）

| 項目 | 說明 |
|------|------|
| `Products.java` | 移除已遷移欄位（price/stock/version 移至 branch_inventory）|
| `GlobalArea.java` | `country → regionsId` 欄位更新 |
| `ProductsDao.java` | 重寫查詢方法（active/toggleActive）|
| `GlobalAreaDao.java` | INSERT 語法修正（regions_id）|
| `BranchInventoryDao.java` | 新增 `findByGlobalAreaId()` |
| `CreateGlobalAreaReq.java` | `country → regionsId` |
| `GlobalAreaService.java` | 修正 insert 呼叫 |
| 後端 DTO（res/） | `ProductVO`, `ProductsRes`, `BranchInventoryVO`, `BranchInventoryRes` |
| 後端 Req | `CreateProductReq`, `UpdateProductReq`, `ToggleProductReq`, `UpdateBranchInventoryReq` |
| `ProductsService.java` | 完整實作（getActiveMenu/getAll/getImage/create/update/toggle）|
| `BranchInventoryService.java` | 完整實作（getByArea/update）|
| `ProductsController.java` | 完整實作（6 個端點）|
| `BranchInventoryController.java` | 完整實作（2 個端點）|
| `api.config.ts` | PRODUCTS/BRANCH_INVENTORY 端點路徑修正 |
| `api.service.ts` | 新增 ProductVO/BranchInventoryVO 介面 + 6+2 個 API 方法（含 MOCK_MODE 支援）|
| `manager-dashboard.component.ts` | 新增 `loadProducts()` + `loadInventory()` 並在 ngOnInit 呼叫 |

### Session 2（前端 API 串接 + Session 過期處理）

| 項目 | 說明 |
|------|------|
| `auth.interceptor.ts` | 新增全域 HTTP 攔截器，401 → `handleSessionExpired()` |
| `auth.service.ts` | 新增 `sessionExpired` signal + `handleSessionExpired()` / `clearSessionExpired()` |
| `app.config.ts` | 掛載 `authInterceptor`（`withInterceptors`）|
| `customer-home.component.ts/html/scss` | Session 過期 Modal（重新登入提示）|
| `manager-dashboard.component.ts` | `DashInventory` 新增 `productId`/`globalAreaId`；`toggleProduct()` 樂觀更新 + API |
| `manager-dashboard.component.ts` | `saveProduct()` 分支 `createProduct()` / `updateProduct()` API 呼叫 |
| `manager-dashboard.component.ts` | `confirmAdjustInventory()` 新增 `updateBranchInventory()` API 呼叫 |
| `api.service.ts` — `CreateGlobalAreaReq` | `country: string` → `regionsId: number` |
| `api.service.ts` — `UpdateGlobalAreaReq` | `country: string` → `regionsId: number` |
| `api.service.ts` — `GlobalAreaVO` | 新增 `regionsId: number` 欄位 |
| `manager-dashboard.component.ts` | `DashBranch` 新增 `regionsId`；`getBranchCountryName()` 輔助方法 |
| `manager-dashboard.component.ts` | `branchDraft` / `editBranchDraft` 改用 `regionsId`；saveBranch/saveEditBranch 更新 |
| `manager-dashboard.component.html` | 新增/編輯分店 Modal：國家下拉改為動態 `@for (t of taxes())` |

---

> **說明：** 所有 API 方法均支援 `MOCK_MODE`（`api.config.ts` 中的 `MOCK_MODE: true`）。
> Demo 時保持 `MOCK_MODE: true`，API 不發出 HTTP 請求，直接回傳假資料。
> 切換真實後端時改為 `MOCK_MODE: false` 並設定 `BASE_URL` 即可。
