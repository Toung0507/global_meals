# 顧客端功能問題追蹤

> 路徑說明：
> - **前端**：`c:\Users\齊\Desktop\global_meals\src\app\`
> - **後端**：`C:\Users\齊\Desktop\global_meals_gradle\src\main\java\com\example\global_meals_gradle\`
> - **SQL**：`C:\Users\齊\Desktop\(學校本地)20260423.sql`

---

## 問題 1：輪播圖「查看菜單」按鈕

### 1a. 按鈕文字改為「前往」

**問題描述**：三張輪播卡片的按鈕文字均來自 `lang.viewMenu`，應改為「前往」（以及對應多語系版本）。

### 1b. 第三張卡片描述寫的是「日式烤雞串」但菜單中不存在此品項

**問題描述**：第三張輪播卡（主廚推薦）的描述文字 `lang.heroChefItemName` 顯示為「日式烤雞串」，但菜單中根本沒有此商品。首頁下方主廚推薦區塊實際顯示的三個品項是：**招牌滷肉飯**、**古早味排骨飯**、**蚵仔麵線**。

### 1c. 點擊「前往」應捲動至主廚推薦區塊

**問題描述**：目前三張卡片的按鈕都呼叫 `setTab('menu')`，只是切換到菜單頁籤，沒有自動定位到主廚推薦區塊。

### 相關檔案

| 層 | 檔案 | 說明 |
|---|---|---|
| 前端 | [customer-home.component.html](src/app/customer-home/customer-home.component.html#L498) | 第一張卡片按鈕 `{{ lang.viewMenu }}`（L498） |
| 前端 | [customer-home.component.html](src/app/customer-home/customer-home.component.html#L521) | 第二張卡片按鈕（L521） |
| 前端 | [customer-home.component.html](src/app/customer-home/customer-home.component.html#L544) | 第三張卡片按鈕（L544），描述文字 `lang.heroChefItemName`（L542） |
| 前端 | [customer-home.component.html](src/app/customer-home/customer-home.component.html#L664) | 首頁主廚推薦區塊（L664~L712），實際三個品項：招牌滷肉飯、古早味排骨飯、蚵仔麵線 |
| 前端 | [customer-home.component.ts](src/app/customer-home/customer-home.component.ts) | `setTab('menu')` — 若要捲動需改為 `setTab('home')` 後 `scrollIntoView` 到 `featured-card` |
| 語系 | BranchService / lang | `lang.viewMenu`、`lang.heroChefItemName` 需對應修改 |

**修改方向**：
1. 將語系字典中 `viewMenu` 改為「前往」（繁中）/ Go（英）/ 進む（日）/ 이동（韓）。
2. 將 `lang.heroChefItemName` 從「日式烤雞串」改為「招牌滷肉飯」。
3. 第三張卡片按鈕改為點擊後切換到 `home` 頁籤並 `scrollIntoView` 到 `#featured-section`，或直接保持 `setTab('home')` 並讓使用者向下滾動看到推薦區塊。

---

## 問題 2：今日優惠 — 優惠價未反映到購物車

**問題描述**：首頁「今日優惠」區塊（L751）展示優惠划線價（如阿三陽春麵 ~~$120~~ **$99**），但這些價格是**純前端展示用的硬寫數字**，點擊卡片只執行 `setTab('menu')`，加入購物車時仍以菜單原價計算。`discountedTotal`（L991）是依活動滿額贈品計算，與今日優惠無關。

### 相關檔案

| 層 | 檔案 | 說明 |
|---|---|---|
| 前端 | [customer-home.component.html](src/app/customer-home/customer-home.component.html#L751) | 今日優惠 HTML 區塊（L751~L845），優惠價全為硬寫 |
| 前端 | [customer-home.component.ts](src/app/customer-home/customer-home.component.ts#L991) | `discountedTotal`（L991）— 只計算活動折扣，未考慮今日優惠 |
| 後端 | 無 | 後端無「今日特價」對應欄位或 API |
| SQL | products 表 | 只有 `base_price`，無促銷價欄位 |

**解決方向（二選一）**：
- **方案 A（建議）**：移除「今日優惠」區塊，或改為純展示橫幅（加上「僅供參考，以實際菜單為準」說明）。
- **方案 B（完整實作）**：SQL `products` 表增加 `sale_price` 欄位，後端 API 回傳，前端計算購物車時套用優惠價。

---

## 問題 3：訂單追蹤 — 訂單狀態消失

**問題描述**：`trackingOrder` 是從 `orderService.latestCustomerOrder()` 計算（L1020），後者取 `_orders` Signal 中 `source === 'customer'` 的第一筆。`_orders` 是**記憶體 Signal，不持久化**——頁面重整或切換頁籤後 Signal 重置，`latestCustomerOrder()` 回傳 `null`，追蹤頁面就顯示空白。另外 `order.service.ts` 初始化時的 demo 資料（L39~68）包含了 'waiting'/'cooking' 的假訂單，但這些是 POS 用途，不是顧客訂單（`source !== 'customer'`）。

### 相關檔案

| 層 | 檔案 | 說明 |
|---|---|---|
| 前端 | [customer-home.component.ts](src/app/customer-home/customer-home.component.ts#L1020) | `trackingOrder`（L1020）— computed from `orderService.latestCustomerOrder()` |
| 前端 | [customer-home.component.html](src/app/customer-home/customer-home.component.html#L1809) | 追蹤頁籤 HTML（L1809~），顯示 waiting/cooking/ready/done 步驟條 |
| 前端 | [order.service.ts](src/app/shared/order.service.ts#L109) | `latestCustomerOrder`（L109）— 取 `source === 'customer'` 的第一筆；`_orders` Signal 無 localStorage 持久化 |
| 前端 | [customer-home.component.ts](src/app/customer-home/customer-home.component.ts#L1712) | `_afterOrderSuccess()`（L1712）— 下單成功後寫入 order.service 並開始輪詢廚房狀態 |
| 後端 | [OrdersController.java](../global_meals_gradle/src/main/java/com/example/global_meals_gradle/controller/OrdersController.java#L74) | `GET /orders/today_orders`（L74）— 可用來重建顧客端訂單狀態 |
| SQL | orders 表 | `orders_status`（ENUM: WAITING/COOKING/READY/DONE/CANCELLED/REFUNDED） |

**修改方向**：下單成功後將 `orderId` 存入 `localStorage`；頁面初始化時若有 `orderId` 則呼叫 `GET /orders/today_orders` 重建追蹤狀態。

---

## 問題 4：我的訂單

### 4a. 訂單編號格式不匹配

**問題描述**：`orderHistoryList`（L1134）是硬寫的 Mock 資料，ID 格式為 `LBB-20260115-001`。實際下單後 `generateOrderId()` 產生的格式是 `LBB-YYYYMMDD-XXXX`（四位毫秒尾數），兩者格式雖相似但 Mock 序號是三位數字，且這份 Mock 資料**從不被後端真實訂單更新**。

### 相關檔案

| 層 | 檔案 | 說明 |
|---|---|---|
| 前端 | [customer-home.component.ts](src/app/customer-home/customer-home.component.ts#L1134) | `orderHistoryList` signal（L1134~L1207）— 純 Mock，未串 API |
| 前端 | [order.service.ts](src/app/shared/order.service.ts#L135) | `generateOrderId()`（L135）— 產生 `LBB-YYYYMMDD-XXXX` |
| 前端 | [customer-home.component.html](src/app/customer-home/customer-home.component.html#L2160) | `{{ order.id }}`（L2160）— 顯示訂單編號 |
| 後端 | [OrdersController.java](../global_meals_gradle/src/main/java/com/example/global_meals_gradle/controller/OrdersController.java#L68) | `GET /orders/get_order_by_phone`（L68）— 可用手機號碼查詢會員歷史訂單 |
| SQL | orders 表 | `id`（VARCHAR）— 對應前端 `LBB-YYYYMMDD-XXXX` 格式 |

**修改方向**：改為呼叫 `GET /orders/get_order_by_phone?phone={memberPhone}` 取得真實訂單歷史，替換 Mock `orderHistoryList`。

---

### 4b. 退款申請沒有串接 API

**問題描述**：`submitRefund()`（L1118）有一行 `// ⚠ TODO [API串接點]`，退款申請**只印 console.log，完全沒有呼叫任何後端 API**。後端雖有 `POST /orders/update_status` 可修改訂單狀態為 `REFUNDED`，但目前沒有審核流程，也沒有通知老闆/分店長的機制。

### 相關檔案

| 層 | 檔案 | 說明 |
|---|---|---|
| 前端 | [customer-home.component.ts](src/app/customer-home/customer-home.component.ts#L1118) | `submitRefund()`（L1118）— TODO 未實作，只有 `console.log` |
| 前端 | [customer-home.component.html](src/app/customer-home/customer-home.component.html) | 退款 Modal HTML，選擇退款原因的 checkbox 列表 |
| 後端 | [OrdersController.java](../global_meals_gradle/src/main/java/com/example/global_meals_gradle/controller/OrdersController.java#L50) | `POST /orders/update_status`（L50）— 可用 `RefundedReq` 將訂單狀態改為 REFUNDED |
| 後端 | [RefundedReq.java](../global_meals_gradle/src/main/java/com/example/global_meals_gradle/req/RefundedReq.java) | Request DTO |
| SQL | orders 表 | `orders_status`（REFUNDED） |

**修改方向**：`submitRefund()` 呼叫 `apiService.updateOrderStatus({ orderId, status: 'REFUNDED' })`；退款審核（由誰處理）目前後端沒有審核流程，建議簡化為「申請即退款」或在老闆/分店長後台加一個「待審退款清單」頁面。

---

### 4c. 無「取消訂單」按鈕

**問題描述**：「我的訂單」tab 有「已取消」篩選，但頁面上**找不到任何讓使用者取消訂單的按鈕**。目前 `orderHistoryList` 的取消訂單是 Mock 假資料（`LBB-20260308-007` 硬寫 `status: 'cancelled'`）。

### 相關檔案

| 層 | 檔案 | 說明 |
|---|---|---|
| 前端 | [customer-home.component.ts](src/app/customer-home/customer-home.component.ts#L1190) | Mock 資料第 7 筆 `status: 'cancelled'`（L1190~L1197） |
| 前端 | [customer-home.component.html](src/app/customer-home/customer-home.component.html#L1999) | 「已取消」篩選 tab（L1999~L2003） |
| 後端 | [OrdersController.java](../global_meals_gradle/src/main/java/com/example/global_meals_gradle/controller/OrdersController.java#L50) | `POST /orders/update_status`，狀態可設 `CANCELLED` |
| SQL | orders 表 | `orders_status`（CANCELLED） |

**修改方向**：在訂單卡片（`waiting` 狀態時）加上「取消訂單」按鈕，呼叫 `POST /orders/update_status` 將狀態設為 `CANCELLED`。

---

## 問題 5：活動專區 — 圖片未連動

**問題描述**：活動專區 `PROMO_DISPLAY`（L1442）從 `GET /promotions/list` API 載入後，圖片欄位取自 `p.promotionImg`（Base64）。但目前 SQL `promotions.promotion_img` 欄位**全為 NULL**（參見前一份問題追蹤文件問題 3），導致圖片顯示空白或破圖。即使之後透過「編輯資訊」面板上傳了圖片，也需確認 `[src]="promo.image"` 能正確顯示。

### 相關檔案

| 層 | 檔案 | 說明 |
|---|---|---|
| 前端 | [customer-home.component.ts](src/app/customer-home/customer-home.component.ts#L1442) | `PROMO_DISPLAY` 從 API 更新（L1442），圖片取 `p.promotionImg` |
| 前端 | [customer-home.component.html](src/app/customer-home/customer-home.component.html#L2354) | 活動卡片圖片 `[src]="promo.image"`（L2354~L2361） |
| 前端 | [customer-home.component.html](src/app/customer-home/customer-home.component.html#L2374) | 活動詳情 Modal 圖片（L2374~L2382） |
| 後端 | [PromotionsManageService.java](../global_meals_gradle/src/main/java/com/example/global_meals_gradle/service/PromotionsManageService.java#L278) | `updatePromotionInfo()` — 圖片以 Base64 解碼後存 BLOB |
| SQL | promotions 表 | `promotion_img` BLOB，目前全為 NULL |

**修改方向**：先解決前一份問題追蹤的「問題 3」（新增活動時一起傳圖片），再確認前端 `[src]` 有加 `data:image/jpeg;base64,` prefix 或做 fallback (`@if (promo.image)`)。

---

## 問題 6：修改個人資料

### 6a. 移除 Email 欄位

**問題描述**：個人資料檢視模式顯示了 Email 欄位（L111），但系統會員是用手機號碼登入，Email 從未被收集。應從頁面完全移除。

### 相關檔案

| 層 | 檔案 | 說明 |
|---|---|---|
| 前端 | [customer-member.component.html](src/app/customer-member/customer-member.component.html#L111) | Email 顯示 `authService.currentUser?.email`（L111），需移除 |

---

### 6b. 手機號碼設為唯讀（不可更動）

**問題描述**：編輯模式下手機號碼欄位（L149~L157）綁定 `[(ngModel)]="editPhone"` 可以修改，但手機號碼是會員身份識別鍵，**不應允許前端自行修改**（後端 `members.phone` 為 UNIQUE KEY）。

### 相關檔案

| 層 | 檔案 | 說明 |
|---|---|---|
| 前端 | [customer-member.component.html](src/app/customer-member/customer-member.component.html#L149) | 手機號碼 input（L149~L157），有 `[(ngModel)]="editPhone"` |
| 前端 | [customer-member.component.ts](src/app/customer-member/customer-member.component.ts#L117) | `saveEdit()` 呼叫 `authService.updateProfile(name, phone, password)`（L124） |
| SQL | members 表 | `phone` UNIQUE KEY |

**修改方向**：將手機號碼 input 改為純顯示（移除 `[(ngModel)]`，或加上 `[readonly]="true"` 並用 CSS 反灰樣式）。

---

### 6c. 修改密碼未串接後端 API

**問題描述**：`saveEdit()` 呼叫 `authService.updateProfile()`（L124），後者**只更新 sessionStorage 中的本地物件**，沒有呼叫 `apiService.updateMemberPassword()`。後端 `POST /members/update_password` 需要 `phone + oldPassword + newPassword`，但目前表單只有新密碼欄位（無舊密碼驗證），完全繞過後端。

### 相關檔案

| 層 | 檔案 | 說明 |
|---|---|---|
| 前端 | [customer-member.component.ts](src/app/customer-member/customer-member.component.ts#L117) | `saveEdit()`（L117）— 只改 sessionStorage，未呼叫 API |
| 前端 | [customer-member.component.ts](src/app/customer-member/customer-member.component.ts#L49) | `editPassword`（L49）— 只有新密碼，無舊密碼欄位 |
| 前端 | [customer-member.component.html](src/app/customer-member/customer-member.component.html#L160) | 新密碼 input（L160~L172），缺少「舊密碼」欄位 |
| 前端 | [auth.service.ts](src/app/shared/auth.service.ts#L418) | `updateProfile()`（L418）— 純 sessionStorage 操作 |
| 前端 | [api.service.ts](src/app/shared/api.service.ts#L1015) | `updateMemberPassword(req)`（L1015），POST 到 `lazybaobao/members/update_password` |
| 後端 | [MembersController.java](../global_meals_gradle/src/main/java/com/example/global_meals_gradle/controller/MembersController.java#L68) | `POST /members/update_password`（L68） |
| 後端 | [UpdatePasswordReq.java](../global_meals_gradle/src/main/java/com/example/global_meals_gradle/req/UpdatePasswordReq.java) | 欄位：`phone`、`oldPassword`、`newPassword` |
| SQL | members 表 | `password` VARCHAR 60（BCrypt 雜湊） |

**修改方向**：
1. 表單新增「舊密碼」輸入欄（`editOldPassword`）。
2. `saveEdit()` 中，若新密碼不為空則呼叫 `apiService.updateMemberPassword({ phone: currentUser.phone, oldPassword: editOldPassword, newPassword: editPassword })`。
3. `authService.updateProfile()` 中移除直接修改本地密碼的邏輯。

---

## 附錄：各問題對應 API 端點

| 問題 | API | Method | 後端 Controller |
|---|---|---|---|
| 3 訂單追蹤重建 | `/lazybaobao/orders/today_orders` | GET | `OrdersController` |
| 4a 真實訂單歷史 | `/lazybaobao/orders/get_order_by_phone` | GET | `OrdersController` |
| 4b 退款申請 | `/lazybaobao/orders/update_status` | POST | `OrdersController` |
| 4c 取消訂單 | `/lazybaobao/orders/update_status` | POST | `OrdersController` |
| 5 活動圖片 | `/lazybaobao/promotions/update_info` | POST | `PromotionsManageController` |
| 6c 修改密碼 | `/lazybaobao/members/update_password` | POST | `MembersController` |
